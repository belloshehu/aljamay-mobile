name: Create Production Build

on:
  push:
    branches: ["main"]

  jobs:
    build_android:
      type: build
      params:
        platform: android

    # build_ios:
    #   type: build
    #   params:
    #     platform: ios
